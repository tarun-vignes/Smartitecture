<Window x:Class="SmartitectureSimple.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings - Smartitecture AI Agent" 
        Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        Background="#0D1117"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style x:Key="SettingsButton" TargetType="Button">
            <Setter Property="Background" Value="#1C2128"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#21262D"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#161B22"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SettingsComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#161B22"/>
            <Setter Property="Foreground" Value="#E6EDF3"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#1C2128" CornerRadius="8" Padding="15" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="⚙️ Settings" 
                           FontSize="20" FontWeight="Bold" Foreground="#58A6FF"/>
                <TextBlock Text="Customize your Smartitecture AI Agent experience" 
                           FontSize="12" Foreground="#8B949E" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- Settings Content -->
        <Border Grid.Row="1" Background="#1C2128" CornerRadius="8" Padding="20">
            <StackPanel>
                <!-- Theme Settings -->
                <TextBlock Text="🎨 Appearance" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>
                
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="Theme:" FontSize="14" Foreground="#E6EDF3" 
                               VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <ComboBox x:Name="ThemeComboBox" Grid.Column="1" 
                              Style="{StaticResource SettingsComboBox}"
                              SelectionChanged="ThemeComboBox_SelectionChanged">
                        <ComboBoxItem Content="🌙 Dark Theme" Tag="Dark"/>
                        <ComboBoxItem Content="☀️ Light Theme" Tag="Light"/>
                        <ComboBoxItem Content="🖥️ System Theme" Tag="System"/>
                    </ComboBox>
                </Grid>
                
                <!-- Backend Settings -->
                <TextBlock Text="🔧 Backend Configuration" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,20,0,15"/>
                
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="Auto-start Backend:" FontSize="14" Foreground="#E6EDF3" 
                               VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <CheckBox x:Name="AutoStartCheckBox" Grid.Column="1" 
                              Content="Start backend automatically when app launches"
                              Foreground="#E6EDF3" VerticalAlignment="Center"/>
                </Grid>
                
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="Backend Port:" FontSize="14" Foreground="#E6EDF3" 
                               VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <TextBox x:Name="PortTextBox" Grid.Column="1" 
                             Background="#161B22" Foreground="#E6EDF3" BorderBrush="#30363D"
                             Text="8001" Padding="8,6"/>
                </Grid>
                
                <!-- About Section -->
                <TextBlock Text="ℹ️ About" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,20,0,15"/>
                
                <TextBlock FontSize="12" Foreground="#8B949E" TextWrapping="Wrap">
                    <Run Text="Smartitecture AI Agent v1.0"/>
                    <LineBreak/>
                    <Run Text="Advanced ReAct framework with Windows automation"/>
                    <LineBreak/>
                    <Run Text="Built with .NET 8.0 and Python backend"/>
                </TextBlock>
            </StackPanel>
        </Border>
        
        <!-- Footer Buttons -->
        <Border Grid.Row="2" Background="#1C2128" CornerRadius="8" Padding="15" Margin="0,20,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="1" Content="💾 Save" 
                        Background="#238636" Style="{StaticResource SettingsButton}"
                        Click="Save_Click" Margin="0,0,10,0" MinWidth="80"/>
                <Button Grid.Column="2" Content="❌ Cancel"
                        Style="{StaticResource SettingsButton}"
                        Click="Cancel_Click" MinWidth="80"/>
            </Grid>
        </Border>
    </Grid>
</Window>
