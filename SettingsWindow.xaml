<Window x:Class="SmartitectureSimple.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="320" Width="460"
        Background="{StaticResource Brush.AppBackground}"
        FontFamily="Segoe UI Variable Text, Segoe UI, Arial"
        Foreground="#F5F7FF">
  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <Border Grid.RowSpan="3" Style="{StaticResource Card.Glass}">
      <StackPanel>
        <TextBlock Text="Appearance &amp; Preferences" Style="{StaticResource Text.H2}" Margin="0,0,0,10"/>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
          <TextBlock Text="Theme:" VerticalAlignment="Center" Margin="0,0,8,0"/>
          <ComboBox x:Name="ThemeComboBox" Width="160" SelectionChanged="ThemeComboBox_SelectionChanged">
            <ComboBoxItem Content="Dark" IsSelected="True"/>
            <ComboBoxItem Content="Light"/>
          </ComboBox>
        </StackPanel>
        <CheckBox x:Name="AutoStartCheckBox" Grid.Row="1" Content="Start with Windows" Margin="0,0,0,12"/>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,0,0,12">
          <TextBlock Text="API Port:" VerticalAlignment="Center" Margin="0,0,8,0"/>
          <TextBox x:Name="PortTextBox" Width="100" Text="8080"/>
        </StackPanel>
      </StackPanel>
    </Border>
    <Button Grid.Row="3" Content="Save" Width="100" HorizontalAlignment="Right" Click="Save_Click" Style="{StaticResource Button.Primary}"/>
  </Grid>
</Window>
