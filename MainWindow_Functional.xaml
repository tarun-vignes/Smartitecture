<Window x:Class="SmartitectureSimple.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Smartitecture - AI Windows Automation Agent" 
        Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#0D1117">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" Text="🚀 Smartitecture - AI Windows Automation Agent"
                   FontSize="24" FontWeight="Bold" Foreground="White"
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>
        
        <!-- Status -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
            <Ellipse x:Name="StatusIndicator" Width="12" Height="12" Fill="Red" Margin="0,0,10,0"/>
            <TextBlock x:Name="StatusText" Text="Backend Stopped" Foreground="White" FontSize="14"/>
        </StackPanel>
        
        <!-- Output -->
        <ScrollViewer Grid.Row="2" Margin="0,0,0,20">
            <TextBox x:Name="OutputTextBox" 
                     Background="#1C2128" Foreground="White" 
                     BorderBrush="#30363D" BorderThickness="1"
                     Padding="15" FontFamily="Consolas" FontSize="12"
                     IsReadOnly="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Text="🤖 Smartitecture Ready - Start the backend to begin automation"/>
        </ScrollViewer>
        
        <!-- Input -->
        <Grid Grid.Row="3" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox x:Name="InputTextBox" Grid.Column="0"
                     Background="#1C2128" Foreground="White"
                     BorderBrush="#30363D" BorderThickness="1"
                     Padding="15" FontSize="14"
                     Text="Take a screenshot then analyze the desktop for productivity improvements"
                     KeyDown="InputTextBox_KeyDown"
                     Margin="0,0,10,0"/>
            
            <Button x:Name="RunAgentButton" Grid.Column="1" Content="🤖 Run Agent"
                    Background="#0969DA" Foreground="White"
                    BorderBrush="#0969DA" BorderThickness="1"
                    Padding="20,15" FontSize="14" FontWeight="Medium"
                    Click="RunAgentButton_Click" IsEnabled="False"/>
        </Grid>
        
        <!-- Controls -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="StartBackendButton" Content="🚀 Start Backend" 
                    Background="#0969DA" Foreground="White"
                    BorderBrush="#0969DA" BorderThickness="1"
                    Padding="15,10" Margin="0,0,10,0"
                    Click="StartBackendButton_Click"/>
            <Button x:Name="StopBackendButton" Content="⏹ Stop Backend"
                    Background="#DA3633" Foreground="White"
                    BorderBrush="#DA3633" BorderThickness="1"
                    Padding="15,10" Margin="0,0,10,0"
                    Click="StopBackendButton_Click" IsEnabled="False"/>
            <Button x:Name="ClearOutputButton" Content="🗑 Clear Output"
                    Background="#6E7681" Foreground="White"
                    BorderBrush="#6E7681" BorderThickness="1"
                    Padding="15,10" Margin="0,0,10,0"
                    Click="ClearOutput_Click"/>
            <Button x:Name="SettingsButton" Content="⚙️ Settings"
                    Background="#6E7681" Foreground="White"
                    BorderBrush="#6E7681" BorderThickness="1"
                    Padding="15,10"
                    Click="SettingsButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
