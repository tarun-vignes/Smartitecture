<Window x:Class="Smartitecture.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:Smartitecture.WPF.ViewModels"
        mc:Ignorable="d"
        Title="Smartitecture Agent"
        Width="800" Height="600"
        WindowStartupLocation="CenterScreen"
        Background="#FF1A1A1A"
        d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel, IsDesignTimeCreatable=True}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBox Text="{Binding UserInput, UpdateSourceTrigger=PropertyChanged}" Width="500" Margin="5"/>
            <Button Content="Run Agent" Command="{Binding RunAgentCommand}" Margin="5"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="5">
            <TextBlock Text="{Binding AgentResponse}" TextWrapping="Wrap"/>
        </ScrollViewer>

        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="Ready"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
